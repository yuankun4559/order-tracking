{
  "private": true,
  "author": "yuankun <yuankun0310@126.com>",
  "scripts": {
    "build:dev": "cross-env UMI_ENV=dev max build",
    "build:prod": "cross-env UMI_ENV=prod max build",
    "build:test": "cross-env UMI_ENV=test max build",
    "build:uat": "cross-env UMI_ENV=uat max build",
    "format": "prettier --cache --write .",
    "postinstall": "max setup",
    "prepare": "husky install",
    "setup": "max setup",
    "start:dev": "cross-env UMI_ENV=dev max dev",
    "start:prod": "cross-env UMI_ENV=prod max dev",
    "start:test": "cross-env UMI_ENV=prod max dev",
    "start:uat": "cross-env UMI_ENV=uat max dev"
  },
  "dependencies": {
    "@ant-design/icons": "^4.7.0",
    "@ant-design/pro-components": "^2.0.1",
    "@antv/g2": "3.4.10",
    "@umijs/max": "^4.0.32",
    "antd": "^4.20.7"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "cross-env": "^7.0.3",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "prettier-plugin-organize-imports": "^2",
    "prettier-plugin-packagejson": "^2",
    "sass-resources-loader": "^2.2.5",
    "typescript": "^4.1.2"
  }
}
